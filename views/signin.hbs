<!DOCTYPE html>
<html lang="{{lang}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <title>{{t 'auth.signingIn'}}</title>
    <style>
        .redirect-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 20px;
            text-align: center;
        }
        .redirect-message {
            margin-top: 30px;
            color: #ffffff;
        }
        .redirect-title {
            font-size: 1.5rem;
            font-weight: 600;
            background: linear-gradient(90.9deg, #D2B8FF 2.74%, #8240FF 102.92%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 10px;
        }
        .redirect-subtitle {
            font-size: 1rem;
            color: #aeb0b4;
            margin-bottom: 20px;
        }
        .redirect-dots {
            display: flex;
            gap: 8px;
            justify-content: center;
            margin-top: 15px;
        }
        .redirect-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: linear-gradient(90.9deg, #D2B8FF 2.74%, #8240FF 102.92%);
            animation: dotPulse 1.4s infinite ease-in-out;
        }
        .redirect-dot:nth-child(1) { animation-delay: 0s; }
        .redirect-dot:nth-child(2) { animation-delay: 0.2s; }
        .redirect-dot:nth-child(3) { animation-delay: 0.4s; }
        @keyframes dotPulse {
            0%, 80%, 100% {
                transform: scale(0.6);
                opacity: 0.5;
            }
            40% {
                transform: scale(1);
                opacity: 1;
            }
        }
    </style>
</head>
<body style="background: #1a1a1a;">
    <!-- Fullscreen overlay with loading spinner -->
    <div class="fullscreen-overlay" id="loadingOverlay">
        <div class="redirect-container">
            <div class="loading-spinner-container">
                <div class="loading-circle shadow" role="status">
                    <span class="visually-hidden d-none">Loading...</span>
                </div>
                <div class="loading-center-content rounded-circle" style="width: 100px; height: 100px; background: white;">
                    <i class="bi bi-chat-heart-fill" style="font-size: 3em; color: #b58afe;"></i>
                </div>
            </div>
            <div class="redirect-message">
                <h2 class="redirect-title">{{t 'auth.signingIn'}}</h2>
                <p class="redirect-subtitle">{{t 'auth.redirectingMessage'}}</p>
                <div class="redirect-dots">
                    <span class="redirect-dot"></span>
                    <span class="redirect-dot"></span>
                    <span class="redirect-dot"></span>
                </div>
            </div>
        </div>
    </div>

    <script src="//code.jquery.com/jquery-3.6.0.min.js"></script>
</body>
{{> dashboard-footer}}
</html>