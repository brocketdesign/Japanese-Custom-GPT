<!-- filepath: /Users/user/Documents/GitHub/Japanese-Custom-GPT/views/singin.hbs -->
<!DOCTYPE html>
<html lang="{{lang}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{translations.signin.title}}</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f8f9fa;
        }
        .signin-container {
            text-align: center;
            padding: 2rem;
            border-radius: 8px;
            background-color: white;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
            max-width: 90%;
            width: 400px;
        }
        .spinner-container {
            margin: 2rem 0;
        }
    </style>
</head>
<body>
    <div class="signin-container">
        <h2 class="mb-4">{{translations.signin.heading}}</h2>
        <p class="mb-3">{{translations.signin.message}}</p>
        
        <div class="spinner-container">
            <div class="spinner-border text-primary" role="status" style="width: 3rem; height: 3rem;">
                <span class="visually-hidden">{{translations.signin.loading}}</span>
            </div>
        </div>
        
        <p>{{translations.signin.wait}}</p>
    </div>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
<style>
    footer,.sticky-bottom{display: none !important;}
</style>
    {{> dashboard-footer}}
</html>